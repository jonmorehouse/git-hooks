# call git counter
.git/scripts/git_counter.sh

# check to see if there is a 
if [[ -f Cakefile ]]; then 
  for i in build pre-commit
  do
    if [[ $(cake | grep "$i" | \wc -l) -gt 0 ]]; then
      cake "$i"
    fi
  done
fi

